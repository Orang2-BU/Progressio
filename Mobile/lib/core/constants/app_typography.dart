/// ─────────────────────────────────────────────────────────────────────────────
/// Progressio — Typography Constants (Dopamine Design System)
/// ─────────────────────────────────────────────────────────────────────────────
class AppTypography {
  AppTypography._();

  // ── Font Families ─────────────────────────────────────────────────────────
  static const String headlineFont = 'PlusJakartaSans';
  static const String bodyFont = 'Inter';
  static const String codeFont = 'JetBrainsMono';

  // ── Font Sizes (sesuai DESIGN.md Type Scale) ──────────────────────────────
  static const double display1 = 32.0;   // Angka skor level up
  static const double heading1 = 24.0;   // Judul hero banner, judul halaman
  static const double heading2 = 20.0;   // Nama skill, modal title
  static const double subtitle1 = 16.0;  // Judul card modul, nama track
  static const double subtitle2 = 14.0;  // Label kategori, chip filter
  static const double body1 = 15.0;      // Materi pembelajaran (reading)
  static const double body2 = 13.0;      // Subteks statistik, deskripsi
  static const double captionTag = 11.0; // Badge "HOT", tag XP "+15 XP"
  static const double codeBlock = 13.0;  // Code snippet, fill-in-blank
}
